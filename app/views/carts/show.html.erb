<h1>Your Cart</h1>

<% @items.each do |item| %>
  <%= image_tag(item.image) %>
  <%= item.title %>
  <%= item.description %>
  <%= item.price %>
  <%= link_to "Remove", cart_path(item), method: :delete %>
<% end %>

<%= @cart.cart_total %>
