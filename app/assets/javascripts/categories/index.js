$(document).ready(function() {
  $('#cycle_forward').on('click', function() {
    $('.toggle').addClass('.disabled');
    //categories.forEach(category){
      // category.data(count, (count -1))
      // if (category.data(count) === 0) {
      //   category.removeClass('.disabled')
      // };
    // });
  });

});
